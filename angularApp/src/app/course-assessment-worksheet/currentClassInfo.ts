export class CurrentClassInfo {
    constructor(public classId: number, public courseId: number, public courseID: string, public syllabus: string,  public section: string, public semester: string, public courseTitle: string, public instructorName: string) { }
  }