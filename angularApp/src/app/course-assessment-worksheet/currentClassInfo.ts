export class CurrentClassInfo {
    constructor(public classId: number, public courseId: number, public courseID: string, public Syllabus: File, public semester: string, public courseTitle: string, public instructorName: string) { }
  }