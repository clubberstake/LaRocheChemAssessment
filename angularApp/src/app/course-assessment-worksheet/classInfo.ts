export class ClassInfo {
    constructor(public courseId: number, public courseID: string, public syllabus: string[], public section: string, public semester: string, public courseTitle: string, public instructorId: number, public instructorName: string) { }
  }